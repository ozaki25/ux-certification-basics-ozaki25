# CLAUDE.md

このリポジトリで作業する際の方針をまとめます。

## プロジェクト概要

**UX検定基礎**（UXインテリジェンス協会主催）の合格を目指す学習コンテンツです。全 6 章 24 レッスンで構成し、1 レッスン 1 トピックで段階的に学べるようにしています。

- サイトは [VitePress](https://vitepress.dev/) で構築し、Vercel にデプロイします。
- 参考にした既存リポジトリ: `color-coordination-training-ozaki25`（同じ仕組み）。

## 試験の前提

- 主催: 一般社団法人 UXインテリジェンス協会（UXIA）。公式サイト: https://www.uxia.or.jp/certification/basic/
- 形式: オンライン CBT・4 択 100 問・100 分。
- 出題範囲: 公式シラバス（大カテゴリ・小カテゴリ・学習目標・重要ワード）。重要ワード（約 80 語）が事実上の出題範囲リスト。
- 公式の推薦図書（4 冊）: 『アフターデジタル2 UXと自由』『UXグロースモデル アフターデジタルを生き抜く実践方法論』『人間中心設計入門』（HCDライブラリー）ほか。正確な一覧は公式サイトで確認する。
- コンテンツを書く前に、最新の公式シラバスと突き合わせて構成・用語を確認する。

## 技術スタック

- VitePress + PWA（`@vite-pwa/vitepress`）
- Mermaid（`vitepress-plugin-mermaid`）— 図解
- vitepress-plugin-tabs — タブ表示
- textlint（`preset-ja-technical-writing`）— 日本語文章の校正
- Vercel Analytics / Speed Insights
- GitHub Actions — `main` と PR でビルドチェック

## ディレクトリ構成

```
docs/
  index.md                     トップページ（カリキュラム目次）
  lessons/lessonNN/index.md    各レッスン（lesson01〜lesson24）
  quiz/                        ドリル（4 択問題）
    types.ts                   型定義・章メタ情報
    data/chapterN.ts           章ごとの問題データ
    chapterN/index.md          章別ドリルページ
    random/ random-5/ random-10/ review/   ランダム・復習ページ
  public/diagrams/             SVG 図版
  .vitepress/
    config.mts                 サイト設定（nav・サイドバー・PWA）
    theme/                     カスタムテーマ（ブランド色・ドリル用 Vue コンポーネント）
scripts/quiz-validate.mjs      ドリルデータの検証スクリプト
```

## 章とレッスンの対応（全 6 章・ドラフト）

公式シラバスの大カテゴリ（UXインテリジェンスの理念 / UX関連基礎知識 / UXプロジェクト計画 / ユーザー理解 / ユーザー要求定義 / UXデザイン具現化 / UXデザイン評価 / UX運用・グロース / UX組織化）を 6 章に再構成しています。

| 章 | テーマ | シラバス大カテゴリ | レッスン |
|----|--------|--------------------|----------|
| 1 | UXインテリジェンスの理念 | UXインテリジェンスの理念 | lesson01〜03 |
| 2 | UX関連基礎知識 | UX関連基礎知識 | lesson04〜08 |
| 3 | UXプロジェクトの計画 | UXプロジェクト計画 | lesson09〜11 |
| 4 | ユーザー理解と要求定義 | ユーザー理解・ユーザー要求定義 | lesson12〜16 |
| 5 | UXデザインの具現化と評価 | UXデザイン具現化・UXデザイン評価 | lesson17〜21 |
| 6 | UX運用・グロースと組織化 | UX運用・グロース・UX組織化 | lesson22〜24 |

### レッスン一覧（ドラフト）

| レッスン | テーマ |
|---|---|
| lesson01 | アフターデジタル時代とUX |
| lesson02 | UXインテリジェンスとは |
| lesson03 | UXと倫理・自由 |
| lesson04 | UXとは（定義と全体像） |
| lesson05 | ユーザビリティとISO規格 |
| lesson06 | 人間中心デザイン（HCD） |
| lesson07 | UD・アクセシビリティ |
| lesson08 | 人間の特性（認知と行動） |
| lesson09 | UXプロジェクトの全体像 |
| lesson10 | プロジェクト計画と体制 |
| lesson11 | 仮説検証のサイクル |
| lesson12 | ユーザー調査の全体像 |
| lesson13 | インタビューと観察 |
| lesson14 | ペルソナ |
| lesson15 | カスタマージャーニーマップ |
| lesson16 | ユーザー要求定義 |
| lesson17 | アイデア発想と構造化 |
| lesson18 | プロトタイピング |
| lesson19 | 専門家によるユーザビリティ評価 |
| lesson20 | ユーザビリティテスト |
| lesson21 | 評価結果の活用 |
| lesson22 | UXグロースの考え方 |
| lesson23 | KPIと効果測定 |
| lesson24 | UX組織化と人材 |

カリキュラムを変更するときは、`docs/.vitepress/config.mts`（サイドバー）・`docs/index.md`（目次）・`docs/quiz/types.ts`（章メタ）・この表の 4 か所を必ず同期させる。

## 執筆スタイル

レッスンを書く・直すときは以下を守ります。

- **対象読者**: UX の知識がほぼない一般社会人。1 レッスン 15 分程度で読める分量。
- **文体**: です・ます体。
- **構成**: 各レッスンは次の順で書く。
  1. `# lessonNN: タイトル — サブタイトル`
  2. `## このレッスンで学ぶこと`（3〜5 個の箇条書き）
  3. 本文（`## セクション` / `### サブセクション`）
  4. `## キーワード`（用語と説明の表）
  5. `## 試験のポイント`（出題されやすい点の箇条書き）
- **図解**: 必要に応じて Mermaid・表・`::: tip` / `::: warning` / `::: info` ブロックを使う。
- **Mermaid のラベル内改行は `\n` を使う**（`<br/>` は使わない。混在させない）。
- **相互参照**は `[lessonNN](/lessons/lessonNN/)` 形式のリンクにする。
- **位置依存の参照を使わない**: 「次のレッスン」「前のレッスン」「前章」など位置を表す語でレッスンを指さない。必ず `[lessonNN]` リンクで指す（カリキュラム再編で壊れないため）。橋渡しの見出しも「次のレッスンへ」ではなく内容ベースにする。
- **長い段落を作らない**: 3 文以上続く説明や複数の論点を含む段落は、短文・箇条書き・表・図に分解して視覚的に追えるようにする。「削って短くする」のではなく「分解して読みやすくする」。
- **ピラミッド構造**: テーマ → 柱（3 つ程度）→ 各柱の詳細、の順で設計する。
- **見出しは体言止め・名詞句**で簡潔に（目次にもなるため）。「〜しよう」「〜してみる」のような話しかけ調にしない。
- **セクション区切りに `---`（水平線）を使わない**。見出し（`##` / `###`）自体が区切りになるため、`---` を重ねると「二重線」のようにくどくなる。セクションは見出しだけで区切る。
- **本文中に em dash（——）を使わない**（文を分けるかカッコを使う）。レッスンタイトルの `タイトル — サブタイトル` の区切りは例外。
- **アクセシビリティを意識**: 図には意味の伝わる `alt` を必ず付け、色だけに依存しない説明を心がける。

## 図版（SVG）のルール

- 図は `docs/public/diagrams/*.svg` に置き、`![alt テキスト](/diagrams/<name>.svg)` で参照する。
- **ファイル名はトピック名**（例: `hcd-cycle.svg`）。`lessonNN` を含めない（カリキュラム再編で破綻するため）。
- **`alt` は省略しない**。図の主旨が伝わる説明を 1〜2 文で書く。`alt="図"` のような無情報文字列は禁止。
- **ダークモード対応必須**: SVG 内に `<style>` を持たせ、`@media (prefers-color-scheme: dark)` で背景・テキスト・補助線の色を切り替える。白背景・黒文字を固定しない。
- 図は視覚的な関係（プロセス・構造・対比）を示す場面に限る。単純な木構造・箇条書きで足りる場合は表や箇条書きを優先する。
- 本文に素の `<script>` / `<style>` を書かない（VitePress は SPA でグローバルスコープを共有するため、他レッスンに影響が漏れる）。ドリルページの `<script setup>`（モジュールスコープ）は可。

## 非公開の参考資料（references/）

- レッスン執筆の一次参考資料として、UXインテリジェンス協会「UXインテリジェンス基礎講座」（全 5 回）の講座資料 PDF をローカルの `references/` に置く。索引は `references/INDEX.md`。
- **非公開情報のため `references/` は git 管理外（.gitignore 済み）。絶対にコミット・プッシュしない。**
- 講座資料の内容（図・文章・構成）をそのまま転載しない。事実・概念を理解した上で、この教材のスタイルで書き直す。
- リモート実行環境ではセッションごとにファイルが消えるため、必要なら再アップロードしてもらう。

## 運用ルール

- 決定事項・方針は会話の中だけに残さず、必ずファイル（CLAUDE.md など）に記録してコミットする。
- レッスン・ドリル・図版を変更したら、コミット前に必ずビルドと検証を通す（後述「変更時のチェック」）。
- ブランチは `main` のみを使う。

## Markdown の太字（強調）について

日本語では `**強調**` の閉じ `**` の直前が全角閉じ括弧（`）」】〉》`）だと、素の markdown-it では強調が描画されず `**` がそのまま表示される（CJK flanking 問題）。例: `**ユーザビリティ（使いやすさ）**`。

このプロジェクトでは `markdown-it-cjk-friendly` を `config.mts` の `markdown.config` で導入してこの問題を解消済み。そのため `**用語（よみ）**` のような書き方をしてもよい。

- ただし**ドリル（quiz）の `question` / `explanation` では `**` は使わない**。QuizCard の `renderText` はバッククォート（`` `code` ``）しか変換せず、`**` はそのまま表示される。ドリルで強調したい場合は文言で工夫する。

## 用語の統一ルール（重要）

教材全体で以下を統一します。

- **UX**: 初出で「UX（ユーザーエクスペリエンス、ユーザー体験）」と定義する。以降は「UX」。
- **UXインテリジェンス**: UXIA の定義に沿って説明する。略記「UXI」を使う場合は初出で定義する。
- **ユーザビリティ**: ISO 9241-11 の定義（有効さ・効率・満足度）を基準にする。
- **人間中心デザイン**: 主表記は「人間中心デザイン（HCD）」。「人間中心設計」と混在させない（引用・書名を除く）。
- **UI と UX を混同しない**: UI は接点（インターフェース）、UX は体験。説明では明確に区別する。
- **シラバスの重要ワード**はシラバスの表記を正とする。独自の言い換えをしない。
- 事実・数値（試験形式・ISO 規格番号など）は公式サイト・シラバス・推薦図書と矛盾しないようにする。新しい数値や用語を持ち込むときは、既存レッスンとの整合を必ず確認する。

## ドリル（quiz）

- 問題データは `docs/quiz/data/chapterN.ts` に `Quiz[]` 型で記述する（`docs/quiz/types.ts` 参照）。
- `id` は全体で一意（`q101` のように「q + 章番号 + 連番」）。`lesson` は実在する `lessonNN` を指す。`choices` は 4 つ・重複なし。`answer` は 0〜3。正解位置は散らす。
- 難易度（`difficulty`）は `easy` / `normal` / `hard` をバランスよく。
- 問題・解説はレッスン本文の事実と一致させる。
- 追加・編集後は必ず `npm run quiz:validate` で検証する。

## よく使うコマンド

```bash
npm run docs:dev       # ローカル開発サーバー
npm run docs:build     # 本番ビルド（Vercel と同じ）
npm run docs:lint      # textlint で日本語を校正
npm run quiz:validate  # ドリルデータの検証
npm run pwa:icons      # logo.svg から PWA アイコンを生成
```

## 変更時のチェック

レッスンやドリルを変更したら、コミット前に最低限これらを通す。

- `npm run docs:lint` がエラーなしで通る
- `npm run docs:build` が成功する
- ドリルを触ったら `npm run quiz:validate` も通る
